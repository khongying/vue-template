// import { ProductStatus } from "@/store/product/product.enum";
// import { UserSystemStatus } from "@/store/user/user.type";

// export class StatusService {
//   public static StatusProduct(status: string): string {
//     if (status) {
//       if (status === ProductStatus.InProgress) {
//         return "กำลังดำเนินการ";
//       } else if (status === ProductStatus.Cancel) {
//         return "ยกเลิก";
//       } else if (status === ProductStatus.Success) {
//         return "สำเร็จ";
//       } else {
//         return "-";
//       }
//     } else {
//       return "-";
//     }
//   }

//   public static UserStatus(status: string): string {
//     if (status) {
//       if (status === UserSystemStatus.Inactive) {
//         return "ไม่ใช้งาน";
//       } else if (status === UserSystemStatus.Active) {
//         return "ใช้งาน";
//       } else {
//         return "-";
//       }
//     } else {
//       return "-";
//     }
//   }
// }
